<?php var_dump($_POST); 

/*
function hasDroit($nomPermission){
    //check le role
    if(admin)
    return true;

    if(pilote && $nomPermission == 'GERER_ETUDIANT')
        retun false;
    if(pilote && $nomPermission == 'GERER_ENTREPRISE')
        retun true;

    //if(select * from permission where idUser = session iduser and droit = $nomPermission == 1)
    //return true;

    return false;
}*/

?>


